<% if current_user.id < 4 %>
    <ul style="width=100%; text-align:center; margin:0 auto; list-style:none;">
    <% @photos.each do |photo| %>
        <li id="<%= photo.id %>" style="float:left;">
            <img src="<%= photo.image.url(:thumbnail) %>" width="10%" />
        </li>
    <% end %>
    </ul>
<% else %>
    <h1>You are not authorized to view this page</h1>
<% end %>
